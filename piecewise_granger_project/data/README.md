# Data

No large external raw dataset is required. The experiments generate synthetic nonlinear VAR and generalized Lorenz time series from code in `src/`.

Generated outputs are written to `results/`.
